[Unit]
Description=FlashLab Next.js Frontend
After=network.target

[Service]
User=www-data
WorkingDirectory=/opt/flashlab/frontend
ExecStart=/usr/bin/npm start
Restart=always
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
